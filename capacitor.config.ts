import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.prueba3hibrido',
  appName: 'prueba3hibrido',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
